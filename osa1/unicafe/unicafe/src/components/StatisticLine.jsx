/* eslint-disable react/prop-types */
const StatisticLine = (props) => {
    return (
        <tr>
            <td>{props.name}</td>
            <td>{props.title}</td>
        </tr>
        
    )
}

export default StatisticLine;